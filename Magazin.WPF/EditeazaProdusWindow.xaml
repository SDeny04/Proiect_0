<Window x:Class="Magazin.WPF.EditeazaProdusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editare Produs" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        SizeToContent="Height"
        Background="#0f172a">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="6" BorderBrush="#334155" BorderThickness="1" Background="#1e293b">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3b82f6"/>
                            </Trigger>
                            <Trigger Property="Validation.HasError" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#ef4444"/>
                                <Setter Property="ToolTip" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Height" Value="38"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#cbd5e1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#cbd5e1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#3b82f6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2563eb"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#334155"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Editare Produs" FontSize="20" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Modificați detaliile produsului selectat." Foreground="#64748b" FontWeight="Normal" FontSize="12"/>
            <Separator Margin="0,10,0,10" Background="#1e293b"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <StackPanel Margin="0,0,0,0">
                <!-- Nume -->
                <TextBlock Text="Nume Produs" />
                <TextBox x:Name="TxtNume" Text="{Binding Nume, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

                <!-- Categorie & Pret -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Categorie" />
                        <ComboBox x:Name="CboCategorie" SelectedItem="{Binding CategorieProdus, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Preț (RON)" />
                        <TextBox x:Name="TxtPret" Text="{Binding PretText, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
                    </StackPanel>
                </Grid>

                <!-- Stoc -->
                <TextBlock Text="Stoc Disponibil" />
                <TextBox x:Name="TxtStoc" Text="{Binding StocText, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

                <!-- Stare Produs (RADIO BUTTONS) -->
                <TextBlock Text="Stare Produs" Margin="0,5,0,5"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <RadioButton x:Name="RadioNou" Content="Nou" IsChecked="{Binding IsNou, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,25,0"/>
                    <RadioButton x:Name="RadioResigilat" Content="Resigilat" IsChecked="{Binding IsResigilat, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- Opțiuni (CHECKBOXES) -->
                <TextBlock Text="Opțiuni Suplimentare" Margin="0,5,0,5" />
                <Border BorderBrush="#1e293b" BorderThickness="1" CornerRadius="6" Background="#111827" Padding="12" Margin="0,0,0,20">
                    <StackPanel>
                        <CheckBox Content="Garanție extinsă" IsChecked="{Binding Garantie}"/>
                        <CheckBox Content="Suport instalare drivere" IsChecked="{Binding SuportDrivere}"/>
                        <CheckBox Content="Livrare Rapidă" IsChecked="{Binding LivrareRapida}"/>
                        <CheckBox Content="Returnare în 14 Zile" IsChecked="{Binding Returnare14Zile}" Margin="0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>

        <!-- Save Button -->
        <StackPanel Grid.Row="2" Margin="0,15,0,0">
            <Button Content="Salvează Modificările" Click="BtnSave_Click" Height="42"/>
        </StackPanel>
    </Grid>
</Window>
