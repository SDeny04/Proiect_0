<Window x:Class="Magazin.WPF.AdaugaProdusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adaugă Produs Nou" Height="550" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#0D1117">

    <Window.Resources>
        <SolidColorBrush x:Key="TextPrimary" Color="#E6EDF3"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#8B949E"/>
        <SolidColorBrush x:Key="BgInput" Color="#161B22"/>
        <SolidColorBrush x:Key="BorderNormal" Color="#30363D"/>
        <SolidColorBrush x:Key="BorderFocus" Color="#58A6FF"/>
        <SolidColorBrush x:Key="TextError" Color="#F85149"/>
        <SolidColorBrush x:Key="BtnPrimary" Color="#238636"/>
        <SolidColorBrush x:Key="BtnPrimaryHover" Color="#2EA043"/>

        <!-- Stil Labels -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <!-- Stil Input -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BgInput}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderNormal}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource BorderFocus}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Stil ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="TextBox.Background" Value="{StaticResource BgInput}"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderNormal}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6,8"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Buton Adauga -->
        <Style x:Key="SaveBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BtnPrimary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="0,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BtnPrimaryHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Padding="30,20">
        <StackPanel>
            <TextBlock Text="Adaugă un Produs Nou" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,24"/>

            <!-- Nume -->
            <TextBlock x:Name="LblNume" Text="Nume Produs*" Style="{StaticResource FormLabel}"/>
            <TextBox x:Name="TxtNume" TextChanged="Input_Changed"/>

            <!-- Categorie & Pret pe un rând -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="LblCategorie" Text="Categorie*" Style="{StaticResource FormLabel}"/>
                    <ComboBox x:Name="CboCategorie" SelectionChanged="CboCategorie_SelectionChanged"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock x:Name="LblPret" Text="Preț (RON)*" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="TxtPret" TextChanged="Input_Changed"/>
                </StackPanel>
            </Grid>

            <!-- Stoc -->
            <TextBlock x:Name="LblStoc" Text="Stoc curent*" Style="{StaticResource FormLabel}"/>
            <TextBox x:Name="TxtStoc" TextChanged="Input_Changed"/>

            <!-- Opțiuni -->
            <TextBlock Text="Opțiuni Extra" Style="{StaticResource FormLabel}"/>
            <Border Background="{StaticResource BgInput}" BorderBrush="{StaticResource BorderNormal}" BorderThickness="1" CornerRadius="6" Padding="15" Margin="0,0,0,20">
                <UniformGrid Columns="2">
                    <CheckBox x:Name="ChkGarantie" Content="Garanție" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                    <CheckBox x:Name="ChkSuportDrivere" Content="Suport Drivere" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                    <CheckBox x:Name="ChkLivrareRapida" Content="Livrare Rapidă" Foreground="{StaticResource TextPrimary}"/>
                    <CheckBox x:Name="ChkReturnare14Zile" Content="Returnare 14 Zile" Foreground="{StaticResource TextPrimary}"/>
                </UniformGrid>
            </Border>

            <!-- Eroare Text (ascuns implicit) -->
            <TextBlock x:Name="TxtError" Foreground="{StaticResource TextError}" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,10" Visibility="Collapsed" />

            <Button Content="Salvează Produs" Style="{StaticResource SaveBtnStyle}" Click="BtnSave_Click"/>
        </StackPanel>
    </Border>
</Window>
